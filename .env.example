# 통합 Compose의 PostgreSQL 비밀번호입니다. 배포 환경에서 반드시 변경하세요.
POSTGRES_PASSWORD=change-me

# 인증 UI가 실제 Supabase에 연결되기 전까지 목업 API로 데모합니다.
VITE_USE_MOCK_API=true

# 빈 값이면 프론트와 같은 origin의 /api/v1을 사용합니다.
VITE_API_ORIGIN=
